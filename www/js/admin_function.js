function dialog_box(message){	$('#dialog').dialog('open');	$('#dialog').html(message);		$('#dialog').dialog({		autoOpen: false,		show: "blind",		hide: "explode",		width: 600,		buttons: {			"Ok": function() {				$(this).dialog("close");				return true;				// $.ajax({ 								//一个Ajax过程					// type: "get", 											// url : "admin.php",					//与此php页面沟通					// data: 'action=media&do=category_del&type=edit&id='+m_c_id, 	//发给php的数据有两项，分别是上面传来的u和p					// success: function(result){			//如果调用php成功						// location.href ="?action=media&do=category";					// }				// });			},			"Cancel": function() {				$(this).dialog("close");				return false;			}		}	});	}